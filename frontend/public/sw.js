// 최소 서비스워커 — "홈 화면에 추가"(PWA 설치) 조건을 만족시키기 위한 용도다.
// 오프라인 캐싱 등 추가 기능은 구현하지 않는다(README 5절 확장 범위 밖, 신규 라이브러리도 쓰지 않음).
self.addEventListener("fetch", () => {
  // 아무 처리도 하지 않는다 — respondWith를 호출하지 않으면 브라우저가 평소대로 네트워크 요청을 처리한다.
});
